
{
  "entities": {
    "playlist": {
      "title": "Playlist",
      "description": "An IPTV playlist source with a name and URL.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The descriptive name of the playlist."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "The URL of the M3U/M3U8 playlist file."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "ISO timestamp of when this entry was last modified."
        }
      },
      "required": ["name", "url", "updatedAt"]
    },
    "channelVisibility": {
      "title": "Channel Visibility",
      "description": "Controls the visibility of a specific channel on the front-end.",
      "type": "object",
      "properties": {
        "channelId": {
          "type": "string",
          "description": "The unique TVG ID of the channel."
        },
        "visible": {
          "type": "boolean",
          "description": "Whether the channel should be visible to users."
        }
      },
      "required": ["channelId", "visible"]
    },
    "heroSlide": {
      "title": "Hero Slide",
      "description": "A single slide for the homepage hero carousel.",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "The main headline of the slide."
        },
        "subtitle": {
          "type": "string",
          "description": "The supporting text below the headline."
        },
        "cta": {
          "type": "string",
          "description": "The text for the call-to-action button."
        },
        "imageId": {
          "type": "string",
          "description": "The ID of the image from placeholder-images.json."
        }
      },
      "required": ["title", "subtitle", "cta", "imageId"]
    },
    "service": {
      "title": "Service",
      "description": "A service offered by the media agency.",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the service."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the service."
        },
        "imageId": {
          "type": "string",
          "description": "The ID of the image from placeholder-images.json."
        }
      },
      "required": ["title", "description", "imageId"]
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "/playlists/{playlistId}": {
      "schema": { "$ref": "#/entities/playlist" },
      "description": "Stores the list of available M3U playlist sources."
    },
    "/settings/{settingId}": {
      "schema": { "type": "object", "properties": { "url": { "type": "string" } } },
      "description": "Global application settings (legacy)."
    },
    "/channel_visibility/{channelId}": {
      "schema": { "$ref": "#/entities/channelVisibility" },
      "description": "Stores visibility status for individual channels, keyed by TVG ID."
    },
    "/hero_slides/{slideId}": {
      "schema": { "$ref": "#/entities/heroSlide" },
      "description": "Stores data for the homepage hero carousel slides."
    },
    "/services/{serviceId}": {
      "schema": { "$ref": "#/entities/service" },
      "description": "Stores data for the media services offered."
    }
  }
}
